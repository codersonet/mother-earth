def test_basic():
    assert 1 + 1 == 2
import requests
"""
def test_api_call():
    response = requests.get('https://api.weatherapi.com/v1/current.json?key=YOUR_API_KEY&q=Delhi')
    assert response.status_code == 200  # Test if the API call is successful
    data = response.json()
    assert 'location' in data  # Test if the response contains 'location'
    assert 'current' in data  # Test if the response contains 'current' weather data
"""
